<%= form_for @post do |f| %>
	<div>
		<%= f.label :title %>
		<%= f.text_field :title, required: true %>
	</div>
	<div>
		<%= f.label :url %>
		<%= f.text_field :url, required: true %>
	</div>
	<div>
		<%= f.submit "Crear url corta" %>
	</div>
<% end %>