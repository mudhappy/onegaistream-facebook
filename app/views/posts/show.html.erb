<div class="content-wrapper">
	<div class="content">
		<p class="visto">Visto <%= @post.counter %> veces</p>

		<% if @video_exist  %>
			<video src="<%= @post.url %>"></video>
		<% else %>
			<p><%= @post.url %></p>
		<% end %>
	</div>
</div>
